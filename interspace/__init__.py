"""Interspace — the interface layer between structured data and your eyes.

Renders graph-shaped data (nodes + edges + clusters) as navigable static
HTML pages. Pluggable input via the Interspace JSON schema.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
